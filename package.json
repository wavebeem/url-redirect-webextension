{
  "private": true,
  "name": "url-redirect-webextension",
  "version": "1.0.0",
  "description": "A WebExtension (Firefox, Chrome) for redirecting URLs",
  "scripts": {
    "start:ff": "web-ext run",
    "build": "web-ext build -i node_modules -i .gitignore -i '*.{afdesign,md}' -i package.json -i package-lock.json --overwrite-dest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wavebeem/url-redirect-webextension.git"
  },
  "keywords": [
    "webextension",
    "firefox",
    "chrome"
  ],
  "author": "Brian Mock <brian@mockbrian.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wavebeem/url-redirect-webextension/issues"
  },
  "homepage": "https://github.com/wavebeem/url-redirect-webextension#readme",
  "devDependencies": {
    "prettier": "^1.19.1",
    "web-ext": "^3.2.1"
  }
}
